services:
  club:
    build: .
    image: club-management:latest
    container_name: club-management
    restart: unless-stopped
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - club-data:/app/data

volumes:
  club-data:
